#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${SCRIPT_DIR}/common.sh"
exec "${SCRIPT_DIR}/run" bash -c 'echo "Engine: /workspace/.cargo-target/release/labzero"; echo "See docs/user_manual.md"'
